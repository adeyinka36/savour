import styled from "styled-components"

type props = {
    message: string
}

const Con  = styled.div`
    


`


const Notification =  ({message}: props) => {



    return(
        <Con>

        </Con>
    )
}